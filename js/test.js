var colorManager = createColorManager()

var blockCanvas = document.getElementById('color_picker-block')
var blockBoard = createBlockBoard(blockCanvas)
var stripCanvas = document.getElementById('color_picker-strip')
var stripBoard = createStripBoard( stripCanvas )
var blockControler = createBlockControler()
var stripControler = createStripControler()

var showArea = createShowArea()

